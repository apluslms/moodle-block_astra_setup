{{!
Derived from A+ (a-plus/edit_course/templates/edit_course/edit_content.html).

Context variables:
* autosetupurl (string) URL to the automatic setup page
* categories (array of stdClass objects), each object has fields 
    - name (string)
    - editurl (string) URL to category edit page
    - has_exercises (bool) true if there is at least one exercise/learning object in the category
    - removeurl (string) URL for deleting the category
* create_category_url (string) URL for creating a new category
* course_modules (array of objects), each object has fields 
    - name (string) with ordinal number
    - status_ready (bool) true if ready
    - status_str (string) status as human-readable string
    - editurl (string) URL for editing the round
    - has_exercises (bool) true if there is at least one exercise/learning object in the round
    - removeurl (string) URL for deleting the round
    - url (string) URL for viewing the round
    - all_exercises (array of objects) includes also hidden exercises, each object has fields 
        * name (string) with ordinal number
        * status_ready (bool) true if status is ready
        * status_str (string) status as human-readable string
        * editurl (string) URL for editing the exercise
        * removeurl (string) URL for deleting the exercise
        * url (string) URL for viewing the exercise
        * submissionlisturl (string) URL to the all submissions list
* create_module_url (string) URL for creating a new exercise round
* renumber_action_url (string) URL for form action that renumbers exercises
}}

<div class="well">
  <a href="{{{ autosetupurl }}}">{{#str}} autosetup, block_stratumtwo_setup {{/str}}</a>
</div>

<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title">{{#str}} exercisecategories, block_stratumtwo_setup {{/str}}</h3>
    </div>
    <table class="table table-striped table-bordered table-condensed">
        <tbody>
            {{# categories }}
            <tr>
                <td>
                    {{ name }}
                </td>
                <td>
                    <a class="btn btn-default btn-xs" href="{{{ editurl }}}">
                        <span class="glyphicon glyphicon-edit" aria-hidden="true"></span>
                        {{#str}} editcategory, block_stratumtwo_setup {{/str}}
                    </a>
                    {{^ has_exercises }}
                    <a class="btn btn-default btn-xs" href="{{{ removeurl }}}">
                        <span class="glyphicon glyphicon-remove" aria-hidden="true"></span> {{#str}} remove, block_stratumtwo_setup {{/str}}
                    </a>
                    {{/ has_exercises }}
                </td>
            </tr>
            {{/ categories }}
            <tr>
                <th colspan="2">
                    <a class="btn btn-primary btn-xs" href="{{{ create_category_url }}}">
                        <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span>
                        {{#str}} addnewcategory, block_stratumtwo_setup {{/str}}
                    </a>
                </th>
            </tr>
        </tbody>
    </table>
</div>


<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title">{{#str}} exerciserounds, block_stratumtwo_setup {{/str}}</h3>
    </div>
    <table class="table table-striped table-bordered table-condensed">
        <tbody>
            {{# course_modules }}
            <tr class="success">
                <th>
                  <strong>{{ name }}</strong>
                  {{^ status_ready }}
                  <span class="label label-default">{{ status_str }}</span>
                  {{/ status_ready }}
                </th>
                <td>
                    <a class="btn btn-default btn-xs" href="{{{ editurl }}}">
                        <span class="glyphicon glyphicon-edit" aria-hidden="true"></span> {{#str}} editmodule, block_stratumtwo_setup {{/str}}
                    </a>
                    {{# has_exercises }}
                    <a class="btn btn-default btn-xs" href="{{{ removeurl }}}">
                        <span class="glyphicon glyphicon-remove" aria-hidden="true"></span> {{#str}} remove, block_stratumtwo_setup {{/str}}
                    </a>
                    {{/ has_exercises }}
                    <a class="btn btn-link btn-xs" href="{{{ url }}}">
                        {{#str}} openround, block_stratumtwo_setup {{/str}}
                    </a>
                </td>
            </tr>
            {{# all_exercises }}
            <tr>
                <td>
                    {{ name }}
                    {{^ status_ready }}
                    <span class="label label-default">{{ status_str }}</span>
                    {{/ status_ready }}
                </td>
                <td>
                    <a class="btn btn-default btn-xs" href="{{{ editurl }}}">
                        <span class="glyphicon glyphicon-edit" aria-hidden="true"></span> {{#str}} editexercise, block_stratumtwo_setup {{/str}}
                    </a>
                    <a class="btn btn-default btn-xs" href="{{{ removeurl }}}">
                        <span class="glyphicon glyphicon-remove" aria-hidden="true"></span> {{#str}} remove, block_stratumtwo_setup {{/str}}
                    </a>
                    <a class="btn btn-link btn-xs" href="{{{ url }}}">
                        {{#str}} openexercise, block_stratumtwo_setup {{/str}}
                    </a>
                    <a class="btn btn-link btn-xs" href="{{{ submissionlisturl }}}">
                        {{#str}} viewsubmissions, mod_stratumtwo {{/str}}
                    </a>
                </td>
            </tr>
            {{/ all_exercises }}
            <tr>
                <td colspan="2">
                    <a class="btn btn-success btn-xs">
                        <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span>
                        {{#str}} addnewlearningobject, block_stratumtwo_setup {{/str}}
                    </a>
                </td>
            </tr>
            {{/ course_modules }}
            <tr>
                <th colspan="2">
                    <a class="btn btn-primary btn-xs" href="{{{ create_module_url }}}">
                        <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span>
                        {{#str}} addnewmodule, block_stratumtwo_setup {{/str}}
                    </a>
                </th>
            </tr>
        </tbody>
    </table>
</div>

<form method="post" action="{{{ renumber_action_url }}}" class="well">
    <div class="form-group">
      <label for="id_module_numbering">{{#str}} modulenumbering, block_stratumtwo_setup {{/str}}</label>
      <select class="form-control" id="id_module_numbering" name="module_numbering">
        <option value="0">{{#str}} nonumbering, block_stratumtwo_setup {{/str}}</option>
        <option value="1">{{#str}} arabicnumbering, block_stratumtwo_setup {{/str}}</option>
        <option value="2">{{#str}} romannumbering, block_stratumtwo_setup {{/str}}</option>
        <option value="3">{{#str}} hiddenarabicnum, block_stratumtwo_setup {{/str}}</option>
      </select>
    </div>
    <div class="form-group">
      <label for="id_content_numbering">{{#str}} contentnumbering, block_stratumtwo_setup {{/str}}</label>
      <select class="form-control" id="id_content_numbering" name="content_numbering">
        <option value="0">{{#str}} nonumbering, block_stratumtwo_setup {{/str}}</option>
        <option value="1">{{#str}} arabicnumbering, block_stratumtwo_setup {{/str}}</option>
        <option value="2">{{#str}} romannumbering, block_stratumtwo_setup {{/str}}</option>
      </select>
    </div>
    
    <button class="btn btn-primary" type="submit" name="save">{{#str}} save, block_stratumtwo_setup {{/str}}</button>
    <button class="btn btn-success" type="submit" name="renumbermodule">{{#str}} renumerateformodules, block_stratumtwo_setup {{/str}}</button>
    <button class="btn btn-success" type="submit" name="renumbercourse">{{#str}} renumerateignoremodules, block_stratumtwo_setup {{/str}}</button>
</form>
